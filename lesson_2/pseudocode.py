# "a function that returns the sum of two numbers"

# Define a function with two parameters
    # function body that takes two inputs and adds them
    # returns the result


# "a function that takes a list of strings and returns 
#  a string that is all those strings concatenated together"

# Define a function that takes a list as a parameter
# function body iterates over list
# while iterating concatenates each index to a new string
# return string


# "a function that takes a list of integers,
# and returns a new list with every other element from the original
# list, starting with the first element"

# define a function that takes a list as a parameter
# body of function interates over every other item in list
# body adds every item it iterates over to new list
# returns new list









# START

# # Given a collection of integers called "numbers"

# SET iterator = 1
# SET savedNumber = value within numbers collection at space 1

# WHILE iterator <= length of numbers
#     SET currentNumber = value within numbers collection at space "iterator"
#     IF currentNumber > savedNumber
#         saveNumber = currentNumber
    
#     iterator = iterator + 1

# PRINT savedNumber

# def find_greatest(numbers):
#     saved_number = numbers[0]
#     index = 1

#     while index < len(numbers):
#         current_number = numbers[index]
#         if current_number > saved_number:
#             saved_number = current_number
    
#         index += 1

#     return saved_number

# my_list = list[1, 24, 5, 0, -1]

# print(find_greatest(my_list))